{
  "name": "novice-wasm",
  "version": "0.0.0",
  "description": "novice project on wasm",
  "type": "module",
  "scripts": {
    "compile": "npx tsc --build ./tsconfig.json",
    "unittest": "mocha --timeout 10000 ./build/tests/*.js",
    "coverage": "npx c8 -- npm run unittest"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.4.2",
    "c8": "^10.0.0",
    "mocha": "^10.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "wasm-tools": "^0.2.1"
  },
  "dependencies": {
    "assert": "^2.1.0"
  },
  "engines": {
    "node": "^16.0.0 || >= 18.0.0"
  }
}
